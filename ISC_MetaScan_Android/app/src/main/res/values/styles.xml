<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorAccent">@color/royalblue</item>
        <item name="android:colorFocusedHighlight">@color/royalblue</item>
        <item name="android:colorControlActivated">@color/royalblue</item>
        <item name="android:colorControlNormal">@color/royalblue</item>
        <item name="android:navigationBarColor">@color/royalblue</item>
        <item name="android:color">@color/royalblue</item>
        <item name="android:colorPrimary">@color/royalblue</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="Widget.AppCompat.ProgressBar.Horizontal" parent="android:Widget.ProgressBar">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@android:drawable/progress_horizontal</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_indeterminate_horizontal</item>
        <item name="android:minHeight">20dip</item>
        <item name="android:maxHeight">20dip</item>
    </style>

    //Added show and hide animations
    <style name="dialogWindowAnim" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_out</item>
    </style>

    //Remove margins and black borders
    <style name="ActionSheetDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:background">@null</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
